# 2026-06-04 工作日志

## 今日完成

### 1. 人工审批节点
- `ApprovalNodeExecutor.cs` — 返回 `NeedsApproval = true`，暂停工作流
- `AiWorkflowRunEntity` 新增 `WaitingApprovalNodeId`、`WaitingApprovalPrompt` 字段
- `WorkflowExecutor` 检查 `NeedsApproval`，发出 `waiting_approval` SSE 事件
- `AiWorkflowService.Approve()` API — 通过/拒绝审批，通过后从断点继续
- 前端：橙色审批栏 + 通过/拒绝按钮
- 修复：`done` 事件的 `waiting_approval` 状态被当作失败

### 2. 可视化条件编辑器
- `ConditionBuilder.vue` — 变量选择、运算符（==/!=/>/</contains/isEmpty）、值输入、AND/OR 组合
- 布局优化：100% 宽度字段，半宽运算符/值，适配 320px 面板

### 3. 连线删除与重连
- VueFlow 配置：`is-edge-selectable`、`edges-updatable`、`deleteKeyCode`
- 双击删除连线、Delete 键删除、选中高亮蓝色
- 工具栏提示"双击连线可删除"

### 4. 连线箭头
- `markerEnd: { type: 'arrow' }` — V 形开放箭头（非实心三角）
- 默认灰色，选中变蓝

### 5. 开始节点输入变量
- 配置面板：自定义变量名、类型（文本/数字/布尔/JSON）、描述
- `StartNodeExecutor` 解析 inputVars，按类型转换值，注入 `global.xxx`
- `buildAvailableVars` 读取 `inputVars` 生成变量列表

### 6. 结束节点输出配置
- 输出模式：文本（拼接）/ JSON（对象）
- 输出变量：字段名 + 来源变量
- `EndNodeExecutor` 按 outputMode 构建输出
- 输出预览：实时显示生成的文本/JSON

### 7. 变量引擎增强
- `VariableEngine.SetVariable()` — 设置单个变量
- `VariableEngine.ResolveValue()` — 解析变量引用到实际值
- `NodeExecutionContext.NodeConfig` — 自动解析 JSON config 为字典

### 8. 前端修复
- `computed` 未导入 — 加入 vue import
- 删除按钮从图标改为红字"删除"按钮 — 更明显

## SQL 迁移（需手动执行）
```sql
ALTER TABLE "ai_workflow_run" ADD COLUMN IF NOT EXISTS "f_waiting_approval_node_id" VARCHAR(50);
ALTER TABLE "ai_workflow_run" ADD COLUMN IF NOT EXISTS "f_waiting_approval_prompt" TEXT;
COMMENT ON COLUMN "ai_workflow_run"."f_waiting_approval_node_id" IS '审批等待节点ID';
COMMENT ON COLUMN "ai_workflow_run"."f_waiting_approval_prompt" IS '审批提示内容';
```

## 关键经验
- VueFlow `default-edge-options` 可全局设置连线样式，不需要每条边单独配置
- VueFlow `markerEnd.type`: `arrow` = V 形开放箭头，`arrowclosed` = 实心三角
- 条件编辑器要适配窄面板（320px），字段用 100% 宽度，运算符和值各占 50%
- `computed` 必须从 vue 显式导入，Vue 3 不会自动注入

## 工作流引擎进度
- Phase 1 (CRUD) ✅
- Phase 2 (Canvas) ✅
- Phase 3 (Variables) ✅
- Phase 4 (Execution Engine) ✅
- Phase 5 (Config UI) ✅ — 条件编辑器、审批节点、开始/结束输入输出
- Phase 6 (Debug) ✅ — 测试面板、节点日志内联、运行详情抽屉
- Phase 7 (Templates) — 待做

## 关键修复（23:28）
- EndNodeExecutor.FindLlmOutput 用 GetNodeOutput 获取原始对象，避免 ToString 丢失数据
- WorkflowExecutor result.Output 已经是 OutputJson，不需要再序列化
- 测试面板：内联显示节点日志（不弹窗），关闭按钮修复
- 所有 PowerShell 编辑文件的操作都导致中文乱码，必须用 edit 工具
