# 2026-06-22 工作日志

## Skill 系统完善
- ImportZip 从 YAML frontmatter + requirements.txt 读取依赖，自动 pip install
- Skill 存储路径改用 `KeyVariable.SystemPath + AiSkill/`
- GetDetail 返回 fileTree，详情页"文件结构"Tab 终端风格展示
- ExportZip 打包整个技能目录，文件名=目录名.zip
- ai_skill 加 f_version 字段，列表/详情/选择器显示版本号
- 旧技能 fallback：先查 SystemPath，不存在再查 wwwroot

## 词云图生成器 Skill
- 创建 `wordcloud-generator-1.0.0` 技能包
- 脚本：jieba 分词 + wordcloud 生成 + matplotlib 输出
- 图片存到 skill 自己的 `output/` 目录（可移植）
- URL: `/api/File/Image/skillOutput/{skillName}/output/{filename}`
- 框架 `GetPathByType` 新增 `skillOutput` 类型
- 路由改 `Image/{type}/{*fileName}` 支持多段路径
- 工具参数定义从通用 `input` 改为具体 `text/width/height`

## Agent 图片显示
- AgentEngine 新增 `ImageUrls` 属性
- 工具返回 `image_url` 时自动追加到回复内容
- 前端 `getImageUrl()` 拼接 apiUrl 前缀
- `parseMessageParts` 三重检测：`[图片:xxx] url`、`![alt](url)`、独立 URL

## 其他修复
- 开场白：Update 方法手动赋值 OpeningStatement
- 搜索按钮：`a-input-search` → `a-input` + `@pressEnter`
- 禁用应用：对话按钮置灰
- 导入 loading：同步 XHR → 异步 + confirmLoading
