# 2026-05-29 日记

## RAG 功能开发（上午 11:30-12:00）

### 第 1 步：LlmClient 扩展 Embedding 接口 ✅
- 新建 `LlmEmbeddingInput.cs` — Embedding 请求 DTO（modelId + texts）
- 新建 `LlmRagChatInput.cs` — RAG 对话 DTO（modelId + datasetId + question + systemPrompt + topK）
- 更新 `ILlmClientService.cs` — 新增 `Embedding()` 和 `RagChat()` 接口
- 更新 `LlmClientService.cs` — 实现 `CallEmbeddingApi()`（POST /v1/embeddings）和 `RagChat()` 完整流程
- 修复 `WeaviateClient.ParseSearchResults()` — 原来是空实现，改为完整解析 GraphQL 响应

### 第 2 步：文档切片服务 ✅
- 新建 `AiDocumentProcessInput.cs` — 切片处理入参（documentId + datasetId + chunkStrategy + chunkSize + chunkOverlap）
- 新建 `AiDocumentProcessOutput.cs` — 处理结果输出
- 新建 `DocumentChunkEntity.cs` — Weaviate 切片实体（extends WeaviateObject）
- 新建 `AiDocumentProcessService.cs` — 完整切片服务（读文档 → 切片 → 向量化 → 存 Weaviate）
  - 支持 3 种切片策略：fixed（固定长度）、paragraph（按段落）、sentence（按句子）
  - 批量向量化（每批 20 条，避免 API 限流）
  - 自动创建 Weaviate Class + 清理旧切片
  - 更新文档状态（wordCount、chunkCount）

### 第 3 步：RAG 检索对话 ✅
- LlmClientService.RagChat() 完整流程：
  1. 查模型 + 提供商 + 知识库配置
  2. 调 Embedding API 向量化问题
  3. Weaviate 向量搜索相似切片（topK）
  4. 构建带上下文的提示词
  5. 调大模型对话
  6. 返回回复 + 来源切片（sources）

### 第 4 步：前端页面 ✅
- `aiDocument/helper/api.ts` — 新增 `processDocument()` API
- `aiDocument/index.vue` — 新增切片处理弹窗（选策略/长度/重叠），调用 Process API
- `aiDataset/index.vue` — 操作列新增「RAG 对话」按钮，跳转 RAG 对话页
- `aiRagChat/index.vue` — 新建 RAG 对话页面（选知识库/模型/TopK，显示来源切片）
- `aiRagChat/helper/api.ts` — 新建（ragChat + getDatasetList + getLlmModels）

### 第 5 步：修复文件读取 ✅
- 问题：原代码直接用 HttpClient 读取本地文件路径，框架里没法用
- 解决：改用 `IFileManager.GetFileStream()` 自动处理本地文件和 OSS 文件
- 支持格式：txt/md/csv/json/xml/html（纯文本）、doc/docx（Aspose.Words）、pdf（FreeSpire.PDF）、xls/xlsx（NPOI）

### 关联文件清单
**后端新增/修改：**
- `Dto/LlmClient/LlmEmbeddingInput.cs` — 新建
- `Dto/LlmClient/LlmRagChatInput.cs` — 新建
- `Dto/AiDocument/AiDocumentProcessInput.cs` — 新建
- `Entity/AiDocument/DocumentChunkEntity.cs` — 新建
- `LlmClient/ILlmClientService.cs` — 新增 Embedding + RagChat 接口
- `LlmClient/LlmClientService.cs` — 新增 Embedding + RagChat 实现，注入 IWeaviateClient + AiDataset 仓储
- `AiDocumentProcess/IAiDocumentProcessService.cs` — 新建
- `AiDocumentProcess/AiDocumentProcessService.cs` — 新建
- `WeaviateClient.cs` — 修复 ParseSearchResults 空实现

## 图片上传模式学习（上午）
- 用户让我读他写好的 AiProvider 图片上传代码（前端 ProviderForm.vue + index.vue，后端 AiProviderService + Mapper），学习他的编码模式
- 后端：Entity 用 `string?` 存 JSON，DTO 输入用 `List<FileControlsModel>?`，输出用 `object?`，Mapper 做 `ToJsonString()` / `ToObject<List<FileControlsModel>>()` 转换
- 前端：`JnpfUploadImg` 组件（pathType="selfPath", isAccount="1", folder="AiProvider"），图片显示用 `apiUrl + item.icon[0].thumbUrl`，编辑加载兼容字符串和数组
- Service 用 `ThenMapperAsync` 反序列化 icon，`CodeGenHelper.SetEmptyStringNull` 清理，`ExecuteCommandWithOptLockAsync` 乐观锁
- 已更新 SKILL.md 和 MEMORY.md，以后遇到图片上传都用这个模式

## Agent 改名（上午）
- 用户让我改名为「神奇海螺」
- 已更新 IDENTITY.md
- 侧边栏显示名需要改 `agents.list[0].identity.name`（在 openclaw.json 配置里），config.patch 对数组不好用，未能完成

## SKILL.md 更新（上午）
- 第三章从「文件上传规范」扩展为「文件/图片上传规范」
- 新增：3.7 前端图片显示、3.8 前端编辑加载、Mapper 完整配置（4 条映射规则）、Service 完整写法
